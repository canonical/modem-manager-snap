summary: Verify mmcli requires root to be executed

execute: |
    ! sudo -u $TEST_USER /snap/bin/modem-manager.mmcli
    sudo -u $TEST_USER /snap/bin/modem-modem.mmcli | grep "ERROR: You're not running mmcli as root which is required."
